/**
 * supabase.ts — STUB v2
 * Supabase supprimé — toutes les fonctionnalités migrent vers Firebase.
 * Ce fichier est conservé uniquement pour éviter les erreurs d'import résiduels.
 */

export const supabase = null;
export const supabaseAdmin = null;
export function requireSupabase(): never {
  throw new Error('[v2] Supabase supprimé — utiliser Firebase.');
}
export function requireSupabaseAdmin(): never {
  throw new Error('[v2] SupabaseAdmin supprimé — utiliser Firebase.');
}
